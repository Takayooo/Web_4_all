<header>

<div class="container header-content">

<h1 class="logo">
<a href="index.php?page=1">Web4All.</a>
<link rel="style.css">
<script src="js/loginmodal.js"></script>
</h1>

<nav class="nav">
<a href="index.php?page=1" class="nav-link">Accueil</a>
<a href="offres.php" class="nav-link">Offres</a>
<a href="entreprises.php" class="nav-link">Entreprises</a>
{% if user %}
    <a href="dashboard.php" class="nav-link">Tableau de bord</a>
{% endif %}
</nav>

<div class="header-actions">


{% if user %}
<div class="user-menu">
    <a href="#" id="user-menu-btn" class="btn btn-white">{{user.prenom}} {{user.nom}} &#x25BE;</a>
    <div id="user-menu-dropdown" class="user-menu-dropdown" aria-label="Menu utilisateur">
        <a href="parametres.php" class="user-menu-item">Paramètres</a>
        <a href="logout.php" class="user-menu-item">Se déconnecter</a>
    </div>
</div>
{% else %}
    <a href="#" id="open-login" class="btn btn-white">Se connecter</a>
{% endif %}

</div>

</div>

</header>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        const btn = document.getElementById('user-menu-btn');
        const dropdown = document.getElementById('user-menu-dropdown');

        if (!btn || !dropdown) return;

        btn.setAttribute('aria-expanded', 'false');

        btn.addEventListener('click', function (event) {
            event.preventDefault();
            event.stopPropagation();
            const isOpen = dropdown.classList.toggle('open');
            btn.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
        });

        document.addEventListener('click', function (event) {
            if (!dropdown.classList.contains('open')) return;
            if (btn.contains(event.target) || dropdown.contains(event.target)) return;
            dropdown.classList.remove('open');
            btn.setAttribute('aria-expanded', 'false');
        });
    });
</script>



<div id="login-modal" class="modal" data-error="{{ hasError ? 'true' : 'false' }}">

<div class="modal-content">

<span class="close">&times;</span>

<h2 style="color:#0d0c6e;">Connexion</h2>
{% if error %}
<p style="color:red;">{{ error }}</p>
{% endif %}

<div class="role-switch">
<button class="role-btn active">Élève</button>
<button class="role-btn">Pilote</button>
<button class="role-btn">Entreprise</button>
</div>

<form action="login.php" method="POST" class="login-form">

<input type="hidden" name="role" id="role-input" value="eleve">

<input type="email" name="email" placeholder="Email" required>

<input type="password" name="password" placeholder="Mot de passe" required>

<button type="submit">Se connecter</button>

</form>

</div>

</div>
